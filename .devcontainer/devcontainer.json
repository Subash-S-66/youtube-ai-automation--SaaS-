{
  "name": "youtube-automation-saas",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.11-bullseye",
  "customizations": {
    "vscode": {
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python"
      },
      "extensions": [
        "ms-python.python"
      ]
    }
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y ffmpeg && python -m pip install --upgrade pip && pip install -r pipeline/requirements.txt && chmod +x scripts/bootstrap_codespaces_env.sh scripts/run_pipeline_codespace.sh scripts/start_codespace_stack.sh scripts/stop_codespace_stack.sh",
  "postStartCommand": "bash scripts/bootstrap_codespaces_env.sh",
  "remoteEnv": {
    "PYTHONPATH": "${containerWorkspaceFolder}/pipeline/src"
  }
}

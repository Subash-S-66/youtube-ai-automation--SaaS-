services:
  clipforge-worker:
    image: ${WORKER_IMAGE:-clipforge-worker:queue-worker-v1}
    env_file:
      - .env
    restart: unless-stopped
    # Uncomment if host ffmpeg hardware acceleration devices are required.
    # devices:
    #   - /dev/dri:/dev/dri

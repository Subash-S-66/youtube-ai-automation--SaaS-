services:
  clipforge-worker:
    build:
      context: ..
      dockerfile: worker/Dockerfile
    image: ${WORKER_IMAGE:-clipforge-worker:queue-worker-local}
    env_file:
      - .env
    restart: unless-stopped
